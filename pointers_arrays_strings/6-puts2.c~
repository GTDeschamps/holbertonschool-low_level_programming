#include"main.h"
#include "2-strlen.c"
/**
 *puts2 - Entry point
 *@s: character
 *Return: void
 */

void puts2(char *str)
{
	int i, end;

	end = _strlen(str);

	for (i = 0; i < end; i += 2)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
