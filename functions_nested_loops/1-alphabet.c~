#include "main.h"
/**
 *print_alphabet - Entry point
 *Return: always 0
 */

void print_alphabet(void)
{
	int alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha = alpha + 1)
	{
		_putchar(alpha);
	}
	_putchar('\n');
	return (0);
}
