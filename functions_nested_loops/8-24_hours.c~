#include "main.h"
/**
 *jack_bauer - Entry point
 *@m - minutes
 *@dm - decimal of minutes
 *@h - hour
 *@dm - decimal of hour
 *Return: time
 */
void jack_bauer(void)
{
	int m;
	int dm;
	int h;
	int dh;

	do {
		do {
			do {
				do {
					m++;
				} while (m <= 9);
				dm++;
				m =0;
			} while (dm <= 6);
			h++;
			dm =0;
			m =0;
		} while (h <= 4);
		dh++;
		h =0;
		dm =0;
		m =0;
	} while (dh <= 2);
}
